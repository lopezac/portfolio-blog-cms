import styled from "styled-components";
import { theme } from "@components/globals";

const SmallGrayLi = styled.li`
  color: ${theme.lightWhite};
  font-size: 0.9rem;
  font-weight: 400;
`;

export default SmallGrayLi;
