import styled from "styled-components";
import { FlexRow } from "@components/globals";

const CenterSmallNav = styled.nav`
  ${FlexRow}
  justify-content: center;
  gap: 10px;
  margin-top: 16px;
`;

export default CenterSmallNav;
